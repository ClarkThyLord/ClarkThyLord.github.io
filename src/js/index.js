// Setup Global(s)
GLOBALS.works = [];
GLOBALS.projects = [];

/**
 * Fetch recent works.
 * @return {undefined} Returns nothing.
 */
function fetch_recent_works() {}

/**
 * Fetch recent works.
 * @return {undefined} Returns nothing.
 */
function fetch_recent_projects() {}
